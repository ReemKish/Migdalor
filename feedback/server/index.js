// Server removed: This file is intentionally left inert.
// The feedback client runs standalone with Vite. If you need a static
// preview of the built client run `npm run build:client` and then
// serve `client/dist` with any static file server.

console.log('feedback/server/index.js - removed; client runs standalone');
